Oracle:
create or replace function mydept(pempno emp.empno%type)
return varchar
is 
erec emp%rowtype;
begin
select * into erec from emp where empno=pempno;
 return erec.deptname;
End;
/

Django:
from django import models
class student(models.Model):
       empid = models.intgerfeild()
       empname = models.charfeild(max_lemgth=20)
